type Query {}

type Document {
    id: ID!
    type: String!
    contents: String!
}

type Application {
    id: ID!
    type: String!
    createTimestamp: String!
    documents: [Document]
}

extend type Query {
    getApplicationById(id: ID!) : Application
}
